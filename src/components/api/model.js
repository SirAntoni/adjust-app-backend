/* const mongoose = require("mongoose");

const { Schema } = mongoose;

const pasarelasSchema = new Schema({
    nombre: { type: String },
    dominio: { type: String },
    credencial_publico: { type: String },
    credencial_id: { type: String },
    credencial_id_iv: { type: String },
    estado: { type: Number },
    fecha_creacion: { type: String },
    fecha_modificacion: { type: String },
}); */

//module.exports.pasarelasModel = mongoose.model("pasarelas", pasarelasSchema, "pasarelas");
